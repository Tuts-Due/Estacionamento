# Copie este arquivo para .env e ajuste os valores
# cp .env.example .env

# Credenciais do MySQL
DB_USER=root
DB_PASS=root

# URL do simulador (padrão quando rodando com docker-compose)
GARAGE_SIMULATOR_URL=http://localhost:8282
